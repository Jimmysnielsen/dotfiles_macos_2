- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig:
    ~/.emacs:
    ~/.emacs.d:
    ~/.howm-keys:
    

- create:
  - ~/Work
  - ~/.vim/undo-history

- shell:

#     - [git submodule update --init --recursive, Installing submodules]

