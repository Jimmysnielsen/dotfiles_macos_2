- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig:
    ~/.emacs:
 #  ~/.emacs.d: # keep it in ~
    ~/.howm-keys:
    ~/.config: ~/.dotfiles/.config
    

- create:
  - ~/Work
  - ~/.vim/undo-history

#- shell: