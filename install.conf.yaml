- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
#	~/.tmux.conf: tmux.conf
#	~/.vim: vim

- create:
  - ~/Work
  - ~/.vim/undo-history

# - shell:
#	- [git submodule update --init --recursive, Installing submodules]